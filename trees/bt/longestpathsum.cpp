//Sum of nodes on the longest path from root to leaf node
#include<bits/stdc++.h>
using namespace std;

struct node{
    int data;
    node * left;
    node* right;
};

int longestsum(node* root)
{
    node*curr=root;


}
